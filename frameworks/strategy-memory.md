# Strategy Memory

Store only auditable records: published content IDs, originating brief/opportunity/pillar IDs, topic/angle/format/channel tags, timestamps, performance observations, derived patterns with sample size/confidence, and strategy version history.

Duplicate detection is a warning system, not semantic truth. Performance patterns decay and should be checked for period/platform/sample-size relevance.
